name: Build and Release

on:
  push:
    tags:
      - 'v*'  # 태그가 'v'로 시작할 때 실행

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install pyinstaller

      - name: Build executable
        run: |
          pyinstaller --onedir --add-data=config/metadata.json:config main.py -y

      - name: Create Release
        uses: actions/github-script@v4
        env:
          GITHUB_TOKEN: ${{ secrets.DURI_TOKEN }}  # GitHub 토큰을 개인 액세스 토큰으로 사용
        with:
          script: |
            const newTag = process.env.GITHUB_REF.replace('refs/tags/', '');  // 태그 이름 가져오기
            const { owner, repo } = github.context.repo;  // owner와 repo 가져오기
            console.log(`Creating release for ${owner}/${repo} with tag ${newTag}`);
            await github.rest.repos.createRelease({
              owner: owner,
              repo: repo,
              tag_name: newTag,
              name: `Release ${newTag}`,
              draft: false,
              prerelease: false
            });
