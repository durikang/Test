파일 추가

bash
코드 복사
git add .
커밋 생성

bash
코드 복사
git commit -m "Add initial version metadata v1.0.0"
태그 추가 초기 버전 태그를 추가하자.

bash
코드 복사
git tag v1.0.0
원격 저장소에 푸시 및 태그 푸시

bash
코드 복사
git push origin master
git push origin v1.0.0